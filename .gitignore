# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# build output
/build
/dist
packages/*/dist
**/.turbo/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local disk caches (sync catalogs, proxied card art)
packages/server/.cache/
.mault-orig-bundle.js
digimon-cat.json

# local data (dataset clones for the TCG importers live here, not in git)
.local/

# server log (start-server.cmd appends here)
server.log

# desktop staging folder (not part of the project)
files for review/
